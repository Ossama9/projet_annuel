{% extends 'base.html.twig' %}

{% block title %}Modifier une catégorie{% endblock %}

{% block body %}
    <h3 class="mb-4">Modifier la catégorie {{ category.name }}</h3>

    <div class="w-25 mb-2">
        {{ include('admin/category/_form.html.twig', {'button_label': 'Sauvegarder'}) }}
    </div>
    {{ include('admin/category/_delete_form.html.twig') }}
    <a class="btn btn-primary mt-2" href="{{ path('admin.category.index') }}">Retour</a>

{% endblock %}
