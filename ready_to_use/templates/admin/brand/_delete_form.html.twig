<form method="post" action="{{ path('admin.brand.delete', {'id': brand.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cette marque ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ brand.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>