{% extends 'base.html.twig' %}

{% block title %}Modifier une marque{% endblock %}

{% block body %}
    <h3 class="mb-4">Modifier une marque</h3>

    <div class="w-25 mb-2">
        {{ include('admin/brand/_form.html.twig', {'button_label': 'Sauvegarder'}) }}
    </div>
    {{ include('admin/brand/_delete_form.html.twig') }}
    <a class="btn btn-primary mt-2" href="{{ path('admin.brand.index') }}">Retour</a>

{% endblock %}
