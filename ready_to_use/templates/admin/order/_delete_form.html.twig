<form method="post" action="{{ path('admin.order.delete', {'id': order.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cette commande ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ order.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>