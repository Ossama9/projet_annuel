{% extends 'base.html.twig' %}

{% block title %}Modifier une commande{% endblock %}

{% block body %}
    <h3 class="mb-4">Modifier une commande</h3>

    <div class="w-25 mb-2">
        {{ include('admin/order/_form.html.twig', {'button_label': 'Sauvegarder'}) }}
    </div>
    {{ include('admin/order/_delete_form.html.twig') }}
    <a class="btn btn-primary mt-2" href="{{ path('admin.order.index') }}">Retour</a>

{% endblock %}
